apiVersion: v1
kind: PersistentVolume

metadata:
  name: mongodb-pv

spec:
  storageClassName: ""
  capacity:
    storage: 5Gi

  accessModes:
    - ReadWriteOnce

  persistentVolumeReclaimPolicy: Retain

  hostPath:
    path: /data/mongodb