apiVersion: v1

kind: Service

metadata:
  name: notification-service
  namespace: cloudcart

spec:

  selector:
    app: notification-service

  ports:

    - port: 8005
      targetPort: 8005

  type: ClusterIP