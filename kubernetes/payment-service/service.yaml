apiVersion: v1

kind: Service

metadata:
  name: payment-service
  namespace: cloudcart

spec:

  selector:
    app: payment-service

  ports:

    - port: 8004
      targetPort: 8004

  type: ClusterIP